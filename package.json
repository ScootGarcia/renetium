{
  "name": "renetium",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://scootgarcia.github.io/renetium",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.22.0",
    "lucide-react": "^0.263.0",
    "gh-pages": "^5.0.0",               // ✅ Make sure this is installed
    "tailwindcss": "^3.3.0",
    // ... any other dependencies like autoprefixer, postcss, etc.
  },
  "scripts": {
    "start": "react-scripts start",     // CRA dev server
    "build": "react-scripts build",     // CRA build
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",       // ✅ Will run before deploy
    "deploy": "gh-pages -d build"       // ✅ Pushes build folder to GitHub Pages
  },
  "devDependencies": {
    // optional: if you use postcss, prettier, eslint, etc.
  }
}
